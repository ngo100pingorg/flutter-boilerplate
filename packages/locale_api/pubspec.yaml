name: locale_api
description: Locale API to manage app locale.

version: 1.0.0+1

environment:
  sdk: ">=3.1.3"
  flutter: ">=3.13.6"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.1

  mockito: ^5.4.2
  test: ^1.24.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
